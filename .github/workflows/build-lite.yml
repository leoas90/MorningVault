name: Build Lite

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: No env block, no secrets, no if condition
        run: echo "build-lite ok at $(date) — proves the issue is in build.yml structure, not platform"
